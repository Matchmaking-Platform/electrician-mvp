# 单包 "workspace" —— 告诉 pnpm 当前目录就是唯一的包,
# 否则严格模式(如 Railway)会报 "packages field missing or empty".
packages:
  - "."

allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  esbuild: true
  msw: true
  prisma: true
  sharp: true
  unrs-resolver: true
